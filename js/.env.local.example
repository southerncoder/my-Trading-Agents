# Local environment overrides for the js project
# Copy this file to .env.local and update values as appropriate. Do NOT commit .env.local.

# LM Studio network settings
LM_STUDIO_HOST=localhost
LM_STUDIO_BASE_URL=http://localhost:1234/v1
LM_STUDIO_ADMIN_URL=http://localhost:1234/admin

# TTL for LM Studio model loaded cache (milliseconds)
LM_STUDIO_MODEL_CACHE_TTL_MS=30000

# Provider defaults
LLM_PROVIDER=lm_studio
LLM_BACKEND_URL=${LM_STUDIO_BASE_URL}

# API keys for cloud providers (optional)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=

# Other optional runtime tuning
RESULTS_DIR=results
LOG_LEVEL=info
