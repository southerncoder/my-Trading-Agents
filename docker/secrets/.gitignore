# =====================================
# DOCKER SECRETS DIRECTORY
# =====================================
# ⚠️  ALL FILES IN THIS DIRECTORY CONTAIN SENSITIVE SECRETS
# ⚠️  NEVER COMMIT SECRET FILES TO VERSION CONTROL
#
# This directory is protected by:
# 1. This local .gitignore (defense-in-depth)
# 2. Root .gitignore patterns (**/secrets/**)
# 3. Gitleaks pre-commit scanning (if enabled)
#
# Only documentation and migration scripts should be committed.
# =====================================

# Ignore ALL text files (contains secrets)
*.txt

# Ignore log files
*.log
migrate-log.txt

# Ignore backup files
*.bak
*.backup

# Allow specific safe files
!README.md
!.gitignore

# Do NOT allow scripts in this directory (scripts should live in /tools or /scripts)
# This prevents accidental commits of executable helpers inside the secrets folder
*.sh
*.ps1
